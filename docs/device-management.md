# Device Management

## Commands

### `builder device list`

List connected devices:

```bash
builder device list
builder device list --json
```

### `builder device info`

Show detailed device information:

```bash
builder device info
builder device info --udid <udid>
builder device info --json
```
