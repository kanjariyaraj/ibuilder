# Screenshots

## Commands

### `builder device screenshot`

Capture device screenshot:

```bash
builder device screenshot
builder device screenshot --output screenshots/
```

Screenshots are saved with timestamps and device metadata.

Default output: `screenshots/screenshot_<device>_<timestamp>.png`
